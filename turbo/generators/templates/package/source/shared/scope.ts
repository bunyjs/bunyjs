import { createMetadataScope, createDecoratorScope } from "@bunyjs/ioc";
 
export const create{{Name}}Metadata = createMetadataScope("{{name}}");
export const create{{Name}}Decorator = createDecoratorScope("{{name}}");
